[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "malicious-import-hook"
version = "0.1.0"
description = "An escrow test fixture that attempts a network connection on import."
