"""The package body itself is inert -- all of this fixture's misbehavior
happens in setup.py at install/build time, not here. That's the point:
this fixture stands in for install-time supply-chain attacks that never
even need the victim to import the package."""
